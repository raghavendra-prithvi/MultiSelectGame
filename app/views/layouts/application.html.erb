<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
  <title>NAME THAT DESIGNER</title>
  <%= stylesheet_link_tag    "application","bootstrap.css","jumbotron-narrow.css", :media => "all" %>
  
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
    

                <% if session[:user_id].nil? %>
                 <%#= link_to image_tag( 'fb_login2.png'), "" %>
                  <%#= link_to  "Login", "/auth/facebook" %>
                <% else %>
                  <% @user = User.find_by_uid(session[:user_id]) %>
                  <% url = '/'+@user.to_param.split(" ").join %>
                    <%=image_tag(@user.img,:style => "float:left; margin-top:0px; margin-top:10px; border-radius: 35px;", :size => '25x25') %>
                  <a  href ='/users/profile?user_id='+<%=@user.id.to_s %> >
                    <%= @user.name %>
                  </a>
                  <div>
                    <%= link_to "Logout", "/sessions/destroy" %>
                    </div>
                 <% end %>  



<%= yield %>

</body>
</html>
